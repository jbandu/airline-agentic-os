# Development: point to local backend
VITE_API_URL=http://localhost:3000

# Production: do not set VITE_API_URL (see .env.production)
# This allows relative URLs (/api) to work with same-domain deployment
